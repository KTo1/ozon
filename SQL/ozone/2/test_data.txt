-- Таблица пользователей
CREATE TABLE "user" (
    id         SERIAL PRIMARY KEY,
    firstname  VARCHAR(100) NOT NULL,
    lastname   VARCHAR(100) NOT NULL,
    birth      DATE NOT NULL
);

-- Таблица покупок
CREATE TABLE purchase (
    id         SERIAL PRIMARY KEY,
    sku        INTEGER NOT NULL,
    price      INTEGER NOT NULL,
    user_id    INTEGER NOT NULL,
    date       DATE NOT NULL
);

-- Таблица банов
CREATE TABLE ban_list (
    user_id    INTEGER PRIMARY KEY,
    date_from  DATE NOT NULL
);



-- =============================================
-- Заполнение таблицы user
-- =============================================
INSERT INTO "user" (id, firstname, lastname, birth) VALUES
(1, 'Ivan',    'Petrov',  '1996-05-01'),
(2, 'Anna',    'Petrova', '1999-06-01'),
(3, 'Anna',    'Petrova', '1990-10-02');

-- =============================================
-- Заполнение таблицы purchase
-- =============================================
INSERT INTO purchase (sku, price, user_id, date) VALUES
(1, 5500, 1, '2021-02-15'),
(1, 5700, 1, '2021-01-15'),
(2, 4000, 1, '2021-02-14'),
(3, 8000, 2, '2021-03-01'),
(4, 400,  2, '2021-03-02');

-- =============================================
-- Заполнение таблицы ban_list
-- =============================================
INSERT INTO ban_list (user_id, date_from) VALUES
(1, '2021-03-08');